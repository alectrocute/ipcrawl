{
  "name": "ipcrawl",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "build:node": "NITRO_PRESET=node-server nuxt build",
    "build:cloudflare": "nuxt build",
    "dev": "NITRO_PRESET=node-server nuxt dev",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "lint": "eslint . --fix",
    "typecheck": "nuxt typecheck",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "cf:dev": "npm run build:cloudflare && wrangler dev",
    "cf:deploy": "npm run build:cloudflare && wrangler deploy",
    "cf:deploy:dev": "npm run build:cloudflare && wrangler deploy --env dev",
    "cf:tail:dev": "wrangler tail --env dev",
    "cf:tail": "wrangler tail"
  },
  "dependencies": {
    "@iconify-json/lucide": "^1.2.108",
    "@iconify-json/simple-icons": "^1.2.83",
    "@nuxt/ui": "^4.7.1",
    "leaflet": "^1.9.4",
    "marked": "^18.0.5",
    "nuxt": "^4.4.6",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "@nuxt/eslint": "^1.15.2",
    "@playwright/test": "^1.61.1",
    "@types/leaflet": "^1.9.21",
    "eslint": "^10.4.0",
    "typescript": "^6.0.3",
    "vue-tsc": "^3.3.0",
    "wrangler": "^4.100.0"
  },
  "packageManager": "pnpm@11.1.3"
}
